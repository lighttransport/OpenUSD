# Agent Instructions

- Never `git push` (including new branches, tags, or `--force`) without asking the user for permission first, even if the change was just created by the agent in the same session.
